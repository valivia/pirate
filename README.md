# card placeholders

- `%NextPlayer%` The next player in the list.
- `%PreviousPlayer%` The previous player in the list.
- `%Player[0-9]%` reference up to 9 different random players.
- `%Self%` The current player.
- `%Rounds%` Takes the number from `duration`.
